{
  "name": "punch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs');['index.html','app.js'].forEach(f=>fs.copyFileSync(f,'Deployment/'+f))\" && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
